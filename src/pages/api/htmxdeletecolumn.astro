---
// #REF astro read post data from htmx

// #REF: htmx redirect usage, return it in the header
return new Response(``, {
  status: 200,
});
---

<!-- <h1 HX-Redirect="www.google.com">htmx is awesome {payload}</h1> -->
